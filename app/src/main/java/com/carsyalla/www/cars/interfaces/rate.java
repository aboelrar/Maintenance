package com.carsyalla.www.cars.interfaces;

import android.content.Context;

public interface rate {
    public interface interfaces {
        interface  View
        {
        }

        interface Model{
            String getdata(String privacy,String comment,String center_id,String reservation_id,String user_id,int rate,Context context);
        }

        interface presenter{
            void getData();
        }
    }
}
